{
  "build": {
    "env": {
      "DATABASE_URL": "${DATABASE_URL}",
      "NEXTAUTH_SECRET": "${NEXTAUTH_SECRET}"
    },
    "commands": [
      "npx prisma generate",
      "npm run build"
    ]
  }
}
